 // onChange event trigger check
    // formComponent.find("#firstName" && "#lastName" && "#email" && '#gender-male' && '#gender-female' && '#datepicker' && "#age").simulate("change", { target: { value: "myValue" } });
    // expect(formComponent.find("#firstName" && "#lastName" && "#email" && '#gender-male' && '#gender-female' && '#datepicker' && "#age").props().value).toBeDefined();

// coverage comment
// jest --watchAll --verbose --coverage --coverageDirectory=output/coverage/jest